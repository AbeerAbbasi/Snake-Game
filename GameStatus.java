/**
 * Enum representing the status of the Game (whether it had just started, it's currently running, or it is over)
 * @author Abeer Abbasi
 * @version 1.0
 */

public enum GameStatus {
    Start,
    Running,
    GameOver
}
